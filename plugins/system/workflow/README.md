# Workflows

System plugin shell for LambChat-owned workflows.

The first implementation slice declares plugin settings, resources, API routes,
frontend contribution points, and plugin-data ownership. Workflow storage,
DSL import, node execution, and invocation integrations are implemented by the
plugin runtime.
